🤖 THE QPAY AGENTIC MASTER PLAN
Target: Transform qpay.qubickle.com from an SMS-syncing script into a highly scalable, "Stripe-like" enterprise payment orchestrator.

🌟 The Core Vision (Context for the AI Agent)
The Mission: QPay is bridging the gap for merchants in South Asia (e.g., Bangladesh) by providing a unified checkout experience for MFS (bKash, Nagad) and Banks.

The Creative Challenge: QPay is currently reliant on Android SMS-scraping. The AI agent must help architect an Adapter Pattern. We need to seamlessly transition from SMS-verification to Direct Bank API verification without the merchant ever noticing the switch.

The Architecture Goal: We are decoupling the monolithic structure. Eventually, api.qpay.qubickle.com (headless, ultra-fast transaction processing) will be completely separate from dashboard.qpay.qubickle.com (heavy analytics and UI).

🛡️ The "Budget-Safe, Zero-Downtime" Rules of Engagement
Dear AI Agent, you must strictly follow these rules:

Atomic Micro-Tasks: You will only perform one complete, testable task at a time. Never start a massive refactor that requires 50 back-and-forth prompts. If the daily token budget runs out, the current deployed code must compile and run flawlessly.

Append, Don't Replace: When adding new architecture (like the new API routes), do not delete the old legacy routes yet. Create api/v1/ routes alongside the legacy system.

Shadow Deployment: When building the new "Double-Entry Ledger," build it to run alongside the old balance update system. We will write to both databases simultaneously until the new ledger is proven stable.

No "Null" States: Never output placeholders like // Add logic here later. Write complete, working functions for the specific micro-task requested.

🗺️ The Micro-Task Execution Plan
Each checkbox represents a distinct session with your AI agent. Complete one, test, commit to Git, deploy, and move to the next.

Sprint 1: API Resuscitation (Fixing the Basics)
Goal: Fix the immediate 404s and establish a working connection with WooCommerce.

[ ] Micro-Task 1.1: Add createPayment logic to ApiController.php and register the POST api/payment/create route in app/Config/Routes.php. (Status: Fixes the 404 error)

[ ] Micro-Task 1.2: Create a getMethods endpoint that queries the database for active gateways and returns a valid JSON array to WooCommerce.

[ ] Micro-Task 1.3: Create a database migration script to generate a payments table with columns for id, merchant_id, amount, currency, status, transaction_id, and timestamps.

Sprint 2: The Security & Reliability Layer
Goal: Stop double-charging and secure the endpoints.

[ ] Micro-Task 2.1 (Idempotency): Update createPayment. Check headers for Idempotency-Key. Query the DB to see if this key exists. If yes, return the previous response. If no, proceed.

[ ] Micro-Task 2.2 (Validation): Create a CodeIgniter Validation rule array for the payment endpoints. Enforce strict type-checking (amount must be float/integer, URLs must be valid format).

[ ] Micro-Task 2.3 (Auth Filter): Move the API-KEY database lookup out of the controller and into a dedicated CI4 Filter (app/Filters/ApiAuth.php). Apply this filter to the api/ route group.

Sprint 3: The Adapter Pattern (Creative AI Task)
Goal: Prepare the system to move away from SMS scraping.

[ ] Micro-Task 3.1: Ask the agent to create a PaymentProviderInterface.php with methods like initiatePayment(), verifyPayment(), and refundPayment().

[ ] Micro-Task 3.2: Ask the agent to build two classes implementing this interface: SmsVerificationAdapter.php (legacy) and DirectApiAdapter.php (new).

[ ] Micro-Task 3.3: Update the GatewayApi library to use a Factory pattern to dynamically load the correct adapter based on the merchant's settings in the DB.

Sprint 4: Developer Experience (DX)
Goal: Give merchants a Stripe-like integration experience.

[ ] Micro-Task 4.1 (Webhooks Part 1): Create a webhook_endpoints table and a UI screen for merchants to add their webhook URLs.

[ ] Micro-Task 4.2 (Webhooks Part 2): Create a background job (or CURL async script) that fires a POST request to the merchant's webhook URL when a transaction status changes to success.

[ ] Micro-Task 4.3 (Test Mode): Modify the API Auth filter. If an API key starts with test_, set a global $isTestMode = true. Update the createPayment function to instantly return success if $isTestMode is true, bypassing the actual payment adapters.

Sprint 5: Financial Integrity (The Double-Entry Ledger)
Goal: Stop losing money to software glitches.

[ ] Micro-Task 5.1: Create a migration for a ledger_entries table (id, account_id, amount, type (credit/debit), transaction_ref, created_at).

[ ] Micro-Task 5.2: Create a LedgerService class. Whenever a successful payment happens, insert a Credit to the Merchant's account and a Debit to the System Clearing account.

[ ] Micro-Task 5.3: Create a helper function getMerchantBalance($merchant_id) that calculates the balance dynamically by summing the ledger entries (Credits - Debits) rather than reading a static "balance" column.

Sprint 6: Infrastructure Separation (Zero-Downtime Migration)
Goal: Scale api.qpay.qubickle.com independently.

[ ] Micro-Task 6.1: Ask the agent to write a Dockerfile and docker-compose.yml optimized for CodeIgniter 4 and PHP 8.2+ FPM.

[ ] Micro-Task 6.2: Set up an external managed Redis instance. Use the AI to configure CI4 to use Redis for session management and caching instead of the local file system.

[ ] Micro-Task 6.3: Move background tasks (like sending emails and webhooks) to a Redis-backed queue system.